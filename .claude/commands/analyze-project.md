# analyze-project

Analyze the current project and generate a structured report.

## Usage

```
/analyze-project [format]
```

## Arguments

- `format` (optional): Output format - "json" or "markdown". Default: "json"

## Example

```
/analyze-project
/analyze-project markdown
```

This will call the MCP tool `analyze_project` to generate a project analysis report.
