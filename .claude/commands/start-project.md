# start-project

Start a new project workflow with vibe-flow.

## Usage

```
/start-project <projectName>
```

## Example

```
/start-project my-new-app
```

This will call the MCP tool `start_project` with the project name.
